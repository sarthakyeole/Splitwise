# Django Splitwise

A simple expense sharing app built using Django.

## Features

- Create groups
- Add expenses
- Auto-split equally
- Show balances per user

## How to run

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
